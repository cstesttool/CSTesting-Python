# Login flow (one test case)
headless=true
goto:https://example.com
# Optional: type and click if you have a form
# username:[name="userName"]=value:mercury
# password:[name="password"]=value:secret
# click=button[type="submit"]
